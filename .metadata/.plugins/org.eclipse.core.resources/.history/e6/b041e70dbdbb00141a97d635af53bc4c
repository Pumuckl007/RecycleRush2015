package main;

public class ConnectionSetter extends Thread{
	private ConnectionIndicator indicator;
	private JButton toteButton;
	public void setIndicator(ConnectionIndicator indicator){
		this.indicator = indicator;
	}
	public void setToteButton(JButton toteButton){
		this.toteButton = toteButton;
	}
	public void run(){
		super.run();
		while(true){
			indicator.setConnected(!indicator.isConnected());
			indicator.repaint();
			try {
				Thread.sleep(2000L);
			} catch (InterruptedException e) {
				e.printStackTrace();
			}
		}
	}
}
